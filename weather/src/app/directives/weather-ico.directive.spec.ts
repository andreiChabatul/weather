import { WeatherIcoDirective } from '../weather-ico.directive';

describe('WeatherIcoDirective', () => {
  it('should create an instance', () => {
    const directive = new WeatherIcoDirective();
    expect(directive).toBeTruthy();
  });
});
