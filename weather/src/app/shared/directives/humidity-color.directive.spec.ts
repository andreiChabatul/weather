import { HumidityColorDirective } from './humidity-color.directive';

describe('HumidityColorDirective', () => {
  it('should create an instance', () => {
    const directive = new HumidityColorDirective();
    expect(directive).toBeTruthy();
  });
});
